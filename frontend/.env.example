INTERNAL_API_URL=http://localhost:8000
# Biến RUNTIME (cố ý không có tiền tố NEXT_PUBLIC_) — chỉ dùng ở Server Component
# nên đổi domain không phải build lại image. Xem ghi chú ở app/layout.tsx.
SITE_URL=http://localhost:3000
